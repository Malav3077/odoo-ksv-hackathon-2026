import { PageHeader } from '@/components/common/page-header'

export const metadata = { title: 'Dashboard — RentEase' }

export default function DashboardPage() {
  return (
    <PageHeader
      title="Dashboard"
      description="Welcome to RentEase — your rental management console."
    />
  )
}
